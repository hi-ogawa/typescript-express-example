# Run `npm run routes` to generate `routes.ts`

get   /home   HomeController  index

post  /users/register   UsersController   register
post  /users/login      UsersController   login
post  /users/new-token  UsersController   newToken
